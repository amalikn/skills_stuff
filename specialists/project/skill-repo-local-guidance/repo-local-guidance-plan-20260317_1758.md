# Repo Local Guidance Plan

Implement a thin root `AGENTS.md` plus `.agents/` local-only guidance pattern for `ansible-wifi`, add local-only ignore rules via `.git/info/exclude`, create a canonical `skill-repo-local-guidance`, and update repo-oriented skills so local repo guidance can be maintained and captured deliberately.
