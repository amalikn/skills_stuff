# Change Checklist

- Run `setup` first.
- Create a fresh snapshot.
- Initialize `history/` if missing.
- Record a history commit only when snapshot contents changed.
- Report the snapshot path and history commit result.
