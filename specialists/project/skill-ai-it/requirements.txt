# Local development venv for the skill-ai-it package itself.
#
# Only ONE shipped script has a third-party dependency: validate_navigation_control_layer.py parses a
# target's context-map.yaml and needs PyYAML. It was supplied invisibly by the host interpreter until
# 2026-09-23, when pinning the runtime made the venv the resolver and `just nav-validate` failed on an
# import nothing had ever declared — relocating a hidden host dependency rather than removing it is
# exactly the failure the runtime-isolation rule describes.
#
# The other four scripts — upgrade_navigation_control_layer.py, check_expected_diff.py,
# selftest_blocks.py and check_governance.py — are standard-library only and must stay that way. The
# governance gate in particular must never fail for environment reasons: a check that cannot run is
# indistinguishable from a check that passes.
PyYAML>=6.0.2
