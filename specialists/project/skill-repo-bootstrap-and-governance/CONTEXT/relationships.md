# Relationships

- Works with global `~/.codex/AGENTS.md` for cross-repo rules.
- Works with repo-local `AGENTS.md` for auto-loaded repo guidance.
- Delegates detailed local-only support-doc layout to `skill-repo-local-guidance` when a thin root plus `.agents/` split is appropriate.
