#!/usr/bin/env bash
# Thin shim -> ansible-grapher project. See SKILL.md for the behavioral contract.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/_resolve_python.sh" "$@"
exec "$RESOLVED_PYTHON" "$SCRIPT_DIR/ansible-grapher" project "$@"
