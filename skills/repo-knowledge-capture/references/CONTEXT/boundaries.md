# Boundaries

- In scope: `.git/info/exclude`, snapshot storage under `~/_ansible/local-knowledge/`, local-only git history under `history/`.
- Out of scope: remote backup, cloud sync, editing tracked files in the target repository, automatic background capture.
